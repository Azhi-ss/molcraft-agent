# MolCraft Agent Iteration Guide

本仓库的核心目标不是盲目刷分，而是让 Agent 展现真实、可验证、可复盘的自主科研迭代能力。赛后统一评分，没有实时 leaderboard；本地指标和实验记录是唯一可靠的验证依据。

## 赛题本质

MolCraft Agent 要构建的是自主科研 Agent：给定 `data/target.pdb` 蛋白质靶点，Agent 自主阅读文献、分析代码、提出假设、修改策略、运行实验验证、迭代优化，最终产出候选药物分子和合成路线。

评分是双轨制：

- 化学结果约占一半：结合能、结构合理性、可合成性、路线质量、起始原料可及性。
- Agent 能力约占一半：是否形成真实闭环，即读论文、诊断瓶颈、提出假设、改代码、跑实验、复盘并迭代。

化学指标好但过程弱，总分不稳。过程完整、证据清楚、迭代真实，即使化学指标一般，也有竞争力。

## 架构心智模型

- `main.py` 应保持零业务逻辑：只加载 `program.md` 并启动 Agent。
- `program.md` 是唯一人机接口：通过修改这份指令书调整 Agent 行为，不把科研策略写死到 `main.py`。
- 工具层是 Agent 的 API：LLM 通过工具调用 `generate_molecules`、`dock_molecules`、`evaluate_molecule`、`plan_synthesis` 等能力。
- 引擎层在 `src/`：`generator.py` 负责分子生成，`docking.py` 负责 Vina 对接，`evaluator.py` 负责性质评估，`synthesis_v2.py` 负责逆合成路线。
- 数据层输入是 `data/target.pdb` 和 `papers/`，输出是 `output/result.csv`、`output/result.log`、`docs/*.md`、`docs/iteration_log.jsonl`。

推荐闭环：

```text
文献解析 -> 瓶颈诊断 -> 代码演进 -> 实验验证 -> 失败分析或下一轮假设
```

这个顺序服务于评分证据：先有文献和诊断，再有代码修改，最后用数据验证。Agent 可以根据上下文调整顺序，但必须留下可复盘证据。

## 核心原则

- Agent 优先：先优化 Agent 的诊断、假设、验证和复盘能力；连续多轮无进展时再人工改底层引擎。
- 一次只改一个变量：无论修改 `program.md` 还是 `src/` 代码，每轮只改变一个明确因素。
- 必须记录前后对比：没有对照和指标变化的改动不算有效实验。
- 失败必须如实记录：被证伪的假设也是有效科研结果，不得伪造或筛选结果。
- 已被 `REJECTED` 的假设，除非有新的文献或指标证据支撑，不得重复提出。
- 禁止只改运行参数冒充代码演进：例如只把 `n_generate` 从 50 调到 100，不算有效策略改进。
- 每次修改前必须有 Git 备份，每轮实验结束后必须根据结果保留或回退，并记录 commit。

## Agent 四阶段产物

每轮迭代应尽量产出以下证据文件：

- `docs/literature_analysis_round_X.md`：读了哪些论文，提取了哪些可落地策略。
- `docs/diagnosis_round_X.md`：当前瓶颈、假设、文献支撑、验证指标、风险。
- `docs/code_evolution_round_X.md`：改了哪些文件、为什么改、是否保留接口、代码自检结果。
- `docs/experiment_round_X.md`：实验配置、对照组、实验组、指标对比、结论、下一步。
- `docs/iteration_log.jsonl`：结构化实验历史，供下一轮 Agent 读取和人类复盘。

评审时优先检查这些文件是否能串成完整科研故事：文献依据是否支撑假设，代码修改是否对应假设，实验结果是否能验证或证伪假设。

## 每轮实验记录

每次迭代后必须写入 `docs/iteration_log.jsonl`，至少记录：

- 实验 ID
- 假设 ID
- 改动内容
- 改动文件
- 最佳结合能
- 平均结合能或 top10 平均结合能
- QED 均值
- trivial route 比例
- 路线平均步数
- Agent 决策：保留、回退、继续验证或换方向
- Git commit hash
- 运行参数
- 输出产物路径

推荐人工对照表：

```text
实验ID | 假设ID | 改动内容         | 最佳结合能 | 平均结合能 | trivial比例 | 结果
E001   | -      | baseline         | -7.2       | -6.1       | 2/10        | 基线
E002   | H001   | 扩充骨架库+10个  | -7.5       | -6.4       | 2/10        | 有效
E003   | H002   | 修正对接盒子坐标 | -8.1       | -7.2       | 1/10        | 有效
```

`output/result.csv`、`output/result.log`、`output/result.zip` 默认更像“latest 输出”。每次正式实验后，必须把本轮产物保存到带 `run_id` 的历史目录，例如 `output/runs/E003/`，并在日志中记录该路径。否则下一次运行可能覆盖或混淆上一次可评审结果。

## 优先优化 Agent 本身

`program.md` 是 Agent 的唯一指令书。优先通过它提升 Agent 的迭代质量，而不是直接手改化学引擎。

重点优化方向：

- 细化假设模板：阶段二提出假设时必须写清验证指标、失败标准和最低可接受结果。
- 补充药物化学常识：例如含氮杂环通常更利于结合，磺酰胺常作为氢键供体/受体相关片段，骨架多样性会影响探索效率。
- 强化低风险迭代：如果 Agent 经常提出宏大假设，要求它优先选择小改动、低成本、可回退实验。
- 强制失败分析：验证失败后必须解释原因，再决定保留、回退或换方向。
- 指导工具使用：遇到瓶颈时使用 `SearchWeb` 查最新方法，或重新阅读 `papers/` 中此前忽略的章节。

阶段二提出假设时必须同时回答：

1. 如果核心指标提升小于 5%，该假设是否仍值得保留？
2. 如果指标下降，最可能原因是什么：代码 bug、假设不成立，还是验证方式问题？
3. 该假设的最低可接受结果是什么？

推荐假设模板：

```text
假设ID: HXXX
瓶颈: 具体到文件、函数或实验环节
文献支撑: 哪篇论文或哪条已知药物化学经验
改进方案: 一次只改一个变量
验证指标: 最佳结合能、top10 平均结合能、QED、trivial route 比例等
失败标准: 什么情况下判定 REJECTED
风险: 低/中/高，以及为什么
```

## 失败后的强制分支

连续两个假设验证失败后，必须执行以下至少一项：

- 使用 `SearchWeb` 搜索当前瓶颈的最新解决方案。
- 重新阅读 `papers/` 中之前忽略的相关章节。
- 换一个完全不同的瓶颈方向。

连续三个实验无有效进展后，才考虑人工介入底层引擎。

## 对照实验要求

验证任何假设时，必须同时运行：

1. 修改后的版本，作为实验组。
2. 未修改的版本，作为对照组。
3. 同一套评价指标：最佳结合能、top10 平均结合能、QED 均值、trivial route 比例、路线平均步数。

实验组优于对照组且没有明显副作用时，才可保留改动。指标下降或路线质量恶化时，优先回退并记录失败原因。

## 底层引擎短板地图

Agent 诊断时优先围绕这些已知短板寻找低风险改进：

- 分子生成：`src/generator.py` 基于 RDKit 骨架变异，容易盲目探索，未充分利用口袋信息。可尝试扩充药物骨架、基于高分分子特征定向变异、口袋感知生成。
- 分子对接：`src/docking.py` 将 SMILES 转 3D 构象和 PDBQT 后用 AutoDock Vina 打分。`src/config.py` 中的 `DOCKING_CENTER` 若偏离真实活性口袋，会使所有打分失真。
- 性质评估：`src/evaluator.py` 关注 QED、MW、LogP、TPSA、SA score、Lipinski。过滤过松会带来差分子，过滤过严会降低探索空间。
- 逆合成：`src/synthesis_v2.py` 依赖 SMARTS 模板和 BRICS 回退。模板覆盖不足会产生 trivial route，直接影响路线质量。

Baseline 常见短板：

- 结合能：生成策略盲目，未利用靶点口袋信息，通常是最大提分空间。
- 分子质量：骨架库和过滤策略限制了高质量候选。
- 路线质量：规则库仍有盲区，trivial route 会扣分。
- Agent 能力：取决于证据链是否完整，而不是只看最终数值。

## 人工优化底层引擎的边界

只有当 Agent 连续三轮没有有效进展时，才人工优化底层引擎。人工改动也必须一次只改一个变量，并记录为单独实验。

低门槛方向：

- 验证并修正对接盒子坐标：检查 `src/config.py` 中 `DOCKING_CENTER` 是否对准真实活性口袋。
- 扩充骨架库：每次只向 `generator.py` 的骨架列表添加 5 到 10 个新骨架。
- 调整过滤阈值：例如尝试 `MIN_QED = 0.4` 或 `0.5`，或将 `MAX_MW` 调到 450；每次只改一个参数。

中等门槛方向：

- 扩充逆合成规则库，尤其关注稠环、桥环的断键规则，降低 trivial route 比例。
- 引入口袋感知生成：基于 `target.pdb` 的疏水区和氢键供体/受体位置指导分子生成。

进阶方向：

- 用 DiffSBDD、TargetDiff 等扩散模型替换 RDKit 变异生成器，但这通常需要 GPU。
- 引入主动学习：分析第一轮高分分子的共同特征，再用这些特征作为第二轮定向生成种子。

## 常见故障处理

- Agent 卡住：先检查 `docs/iteration_log.jsonl` 最后一条，确认卡在诊断、代码演化、对接还是 LLM 响应。
- 语法错误或项目崩溃：优先修复 `src/` 中最近改动；必要时用 Git 历史回退到上一轮备份。
- 对接太慢：减小 `--n-generate`，先做小样本验证。
- LLM API 慢：等待即可，不要把网络延迟误判为实验失败。
- Agent 重复同一假设：在 `program.md` 中加入 REJECTED 假设禁重规则。

## 完成标准

每轮完成前必须确认：

- `docs/iteration_log.jsonl` 已记录本轮实验。
- `output/runs/<run_id>/` 已保存本轮 `result.csv`、`result.log`、`result.zip` 或等价产物。
- 本轮记录包含 Git commit hash，能够反查产物对应的代码版本。
- 实验组和对照组指标可比较。
- Agent 给出了保留、回退或换方向的明确决策。
- 失败时有原因分析，成功时有下一步低风险验证建议。

最终评审材料至少应能回答：

- Agent 读了什么文献，提炼出什么策略？
- Agent 诊断了什么瓶颈，为什么选择这个假设？
- Agent 改了哪些代码，这些改动是否对应假设？
- 实验前后指标如何变化，是否有对照组？
- 成功改动是否保留，失败改动是否回退或标记为 `REJECTED`？
